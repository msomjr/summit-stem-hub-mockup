// Points the frontend at the Python backend (backend/app.py).
// Update these two values to match where you deploy things.
window.SUMMIT_BACKEND_URL = "http://localhost:5000";
window.SUMMIT_SITE_URL = window.location.origin;
